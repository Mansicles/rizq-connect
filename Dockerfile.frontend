# Use a Node image specifically for running the development server and hot-reloading
FROM node:20-alpine

WORKDIR /app

# Copy and install dependencies
COPY package*.json ./
RUN npm install

# Copy the rest of the application code
COPY . .

# React development server default port
EXPOSE 3000

# Command to start the application (used when running this Dockerfile directly)
CMD ["npm", "start"]